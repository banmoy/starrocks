# 设计任务：去掉存算分离 tablet 创建时的 CN 交互

本文件包含设计标准和分阶段 Cloud Agent 提示词。

---

## 设计标准（三个阶段共享）

### 设计目标

去掉存算分离内表 tablet 创建时 FE 与 CN 之间的交互（即 CreateReplicaTask），使 DDL 操作不再依赖 CN 的存在和可用性。

### 优先级栈（从高到低，不可逆转）

1. **核心读写路径的正确性和性能**：首次写入（publish version）、查询、compaction、schema change 执行——这些路径绝不能因为优化 DDL 而退化
2. **升降级兼容性**：方案必须天然适配"先升 CN 后升 FE、先降 FE 后降 CN"的规范；如果方案需要额外脚本、人工步骤或 feature flag 才能兼容，说明设计本身有问题
3. **正确性和完整性**：覆盖所有创建路径和所有下游消费者，不能有遗漏
4. **DDL 路径的可用性和性能提升**：这是设计动机，但优先级排在上面三项之后

在任何 tradeoff 中，高优先级项不可为低优先级项让步。

### 好的设计长什么样

- 用一个统一机制处理所有受影响的路径，而不是逐条路径打补丁
- 下游消费者不需要区分"老 tablet"和"新 tablet"，或者只需要在一个统一的地方处理这个区分
- 兼容性是方案的自然属性，不是额外加的兼容层
- 方案落地后，系统整体变得更简单，而不是更复杂

### 不可接受的设计

- 对每条下游消费者路径逐个加 fallback / 特殊处理——这不是设计，是打补丁
- 把 CN 做的事原样搬到 FE 或其他组件——这不是去掉依赖，是转移依赖
- 引入异步补齐机制来弥补同步正确性的缺失
- 引入后台修复任务来弥补设计闭环的缺失
- 引入人工运维步骤来弥补系统保证的缺失
- 用更多状态、协议、中间态来替代清晰的机制

如果方案中的例外逻辑超过 3 处，先停下来怀疑方向是否错了。

### 从第一性原理出发

不要从"CN 现在做了这 4 件事，怎么在别的地方做"出发。要从这些问题出发：

- 系统本质上需要什么才能让一个新 tablet 可用？
- 当前的哪些步骤是本质需求，哪些只是当前实现选择？
- 有没有一种方式，使得这些本质需求在不写初始 metadata 的情况下也能被满足？

### 工作纪律：事实、推论和未知

在整个设计过程中，严格区分三类信息：

- **事实**：已在文档或代码中直接验证。标注依据（文件名:行号，或文档章节）
- **推论**：基于事实逻辑推导得出。标注推导链
- **未知**：目前无法确认。明确标注，不允许把未知写成结论

如果你不确定某个行为，去代码里验证，不要猜。

---

## 使用方法

分三次启动 Cloud Agent，每次用下面对应阶段的提示词。每次启动前审阅上一阶段的产出，可以在下一阶段的提示词末尾追加你的反馈和修改意见。

---

## 第一阶段：Plan

复制以下内容作为 Cloud Agent 的任务描述：

```
你是一位资深的数据库和分布式系统架构设计师。你需要为 StarRocks 存算分离模式设计一个方案：去掉 tablet 创建时与 CN 的交互。

## 本阶段任务

这是三阶段设计的第一阶段：制定 Plan。

你需要：
1. 完整阅读 docs/dev/lazy-tablet-creation/ 目录下的全部 5 份分析文档（从 00-index.md 到 04-affected-code-paths.md）
2. 结合代码库验证文档中的关键结论，特别是：创建路径是否完整、下游消费者是否有遗漏、升降级分析是否准确
3. 阅读 docs/dev/lazy-tablet-creation/design-prompt.md 中的"设计标准"部分，这是整个设计的验收标准
4. 产出 Plan

## 产出要求

将 Plan 写入 docs/dev/lazy-tablet-creation/design-phase1-plan.md，内容包括：

1. **文档验证结果**：文档中有哪些遗漏、不准确或需要补充的地方。如果没有发现问题，明确说明你验证了哪些关键点
2. **问题本质的理解**：用你自己的话概括这个问题的本质是什么、核心挑战是什么
3. **设计步骤规划**：你打算分几步完成设计、每一步的目标是什么
4. **初步方向思考**：你目前倾向的大方向是什么（简要说明，不展开细节）、有哪些备选方向值得探索
5. **待讨论问题**：需要和我确认的问题、你识别到的关键 tradeoff

## 工作纪律

- 区分事实、推论和未知，标注依据
- 不确定的事去代码里验证，不要猜
- 不要在本阶段展开具体设计方案
```

---

## 第二阶段：核心机制

审阅第一阶段产出后，复制以下内容作为 Cloud Agent 的任务描述（根据需要在末尾追加你的反馈）：

```
你是一位资深的数据库和分布式系统架构设计师。你需要为 StarRocks 存算分离模式设计一个方案：去掉 tablet 创建时与 CN 的交互。

## 本阶段任务

这是三阶段设计的第二阶段：确定核心机制。

前置材料：
- 背景文档：docs/dev/lazy-tablet-creation/ 目录下的 00 到 04 共 5 份文档
- 设计标准：docs/dev/lazy-tablet-creation/design-prompt.md 中的"设计标准"部分
- 第一阶段 Plan：docs/dev/lazy-tablet-creation/design-phase1-plan.md

请先完整阅读以上所有材料，然后完成本阶段任务。

## 产出要求

将核心机制设计写入 docs/dev/lazy-tablet-creation/design-phase2-mechanism.md，内容包括：

1. **核心机制**：整体方向是什么、核心机制是什么、用一句话概括
2. **为什么是这个方向**：你考虑过哪些备选方向、每个备选方向被排除的具体原因
3. **关键路径验证**：用这个核心机制走一遍以下场景，说明具体行为：
   - 新 tablet 的首次写入（publish version）
   - 新 tablet 的首次查询（SELECT on empty table）
   - Schema change 对新 tablet 的执行
   - 跨集群复制到新 tablet
4. **升降级验证**：在"先升 CN 后升 FE"和"先降 FE 后降 CN"的每个中间状态下，核心机制是否天然成立
5. **关键 tradeoff**：列出所有需要取舍的点、你的判断和依据
6. **未知项和风险**：目前无法确认的问题

## 自我检验（必须回答）

在产出末尾，显式回答以下问题：
- 这个方案是真正消除了依赖，还是换了个形式？
- 首次写入、首次查询有没有性能退化？具体退化多少、是否可接受？
- 方案中有多少处"例外处理"？如果超过 3 处，方向是否需要重新审视？
- 方案落地后系统是变简单了还是变复杂了？

## 工作纪律

- 区分事实、推论和未知，标注依据
- 不确定的事去代码里验证，不要猜
- 不要在本阶段展开每条路径的完整处理方式，聚焦核心机制
- 严格遵守 design-prompt.md 中的优先级栈和设计标准
```

---

## 第三阶段：完整设计

审阅第二阶段产出后，复制以下内容作为 Cloud Agent 的任务描述（根据需要在末尾追加你的反馈）：

```
你是一位资深的数据库和分布式系统架构设计师。你需要为 StarRocks 存算分离模式设计一个方案：去掉 tablet 创建时与 CN 的交互。

## 本阶段任务

这是三阶段设计的第三阶段：完整设计。

前置材料：
- 背景文档：docs/dev/lazy-tablet-creation/ 目录下的 00 到 04 共 5 份文档
- 设计标准：docs/dev/lazy-tablet-creation/design-prompt.md 中的"设计标准"部分
- 第一阶段 Plan：docs/dev/lazy-tablet-creation/design-phase1-plan.md
- 第二阶段核心机制：docs/dev/lazy-tablet-creation/design-phase2-mechanism.md

请先完整阅读以上所有材料，然后完成本阶段任务。

## 产出要求

将完整设计写入 docs/dev/lazy-tablet-creation/design-phase3-full.md，内容包括：

1. **方案概述**：核心机制回顾、整体架构变更概述
2. **FE 侧变更**：哪些代码需要改、怎么改、改动的语义是什么
3. **CN/BE 侧变更**：哪些代码需要改、怎么改、改动的语义是什么
4. **每条受影响路径的处理方式**：
   - 04-affected-code-paths.md 中 8 个 Category 的每一条路径如何处理
   - 对于统一机制覆盖的路径，说明为什么被覆盖
   - 对于需要单独处理的路径，说明为什么不能被统一机制覆盖、具体怎么处理
5. **升降级方案**：
   - 升级的每个阶段（CN 新 + FE 老 → CN 新 + FE 新）的行为
   - 降级的每个阶段（FE 老 + CN 新 → FE 老 + CN 老）的行为
   - 是否需要数据迁移或修复工具
6. **配置和监控变更**：哪些配置参数变化、哪些监控指标变化
7. **不需要具体代码实现**，但需要精确到"哪个文件的哪个函数需要什么语义的变更"

## 自我检验（必须回答）

在产出末尾，显式回答以下问题：
- 04-affected-code-paths.md 中的所有路径是否全部覆盖？逐条确认
- 方案中有多少处"例外处理"？每一处的理由是什么？
- 升降级的每个中间状态是否安全？逐状态确认
- 方案落地后系统是变简单了还是变复杂了？从哪些维度判断？

## 工作纪律

- 区分事实、推论和未知，标注依据
- 不确定的事去代码里验证，不要猜
- 严格遵守 design-prompt.md 中的优先级栈和设计标准
- 如果发现核心机制需要调整，在文档开头明确指出，不要悄悄改
```
